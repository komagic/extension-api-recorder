// 实例化配置、工具、处理器、消息类
```ts
const requestProxyConfig = new RequestProxyConfig();
const requestProxyUtils = RequestProxyUtils;
const requestProxyHandle = new RequestProxyHandle(requestProxyConfig);
const messager = new Messager(requestProxyConfig);

```